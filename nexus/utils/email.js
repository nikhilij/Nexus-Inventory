// utils/email.js
class Email {
   send(to, subject, body) {
      // Implementation for sending email
   }

   sendTemplate(to, template, data) {
      // Implementation for sending templated email
   }
}

const email = new Email();
export default email;
