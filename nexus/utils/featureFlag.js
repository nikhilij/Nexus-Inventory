// utils/featureFlag.js
class FeatureFlag {
   isEnabled(flag) {
      // Implementation for checking if feature is enabled
   }

   toggle(flag) {
      // Implementation for toggling feature flag
   }
}

const featureFlag = new FeatureFlag();
export default featureFlag;
