// utils/logger.js
class Logger {
   info(message, meta) {
      // Implementation for info logging
   }

   error(message, meta) {
      // Implementation for error logging
   }

   warn(message, meta) {
      // Implementation for warning logging
   }

   debug(message, meta) {
      // Implementation for debug logging
   }
}

const logger = new Logger();
export default logger;
