// utils/scheduler.js
class Scheduler {
   scheduleJob(name, cronExpression, job) {
      // Implementation for scheduling a job
   }

   cancelJob(name) {
      // Implementation for canceling a job
   }
}

const scheduler = new Scheduler();
export default scheduler;
