// utils/tenant.js
class Tenant {
   getCurrentTenant() {
      // Implementation for getting current tenant
   }

   switchTenant(tenantId) {
      // Implementation for switching tenant
   }
}

const tenant = new Tenant();
export default tenant;
